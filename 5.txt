1. 다음 결과 값은?
  > var a = 42, b;
  > b = ( a++, a );
  > a;
  : ??
  > b;
  : ??
  
2. 레이블이란?

3. 다음 값은 ?
  > [] + {};
  : ??
  > {} + [];
  : ??
  
4. 1번에서 ()를 없애면 b의 값은?

5. 다음 값은?
  > var a = 42;
  > var b = "foo";
  > var c = false;
  > var d = a && b || c ? c || b ? a : c && b : a;
  > d;
  : ???
